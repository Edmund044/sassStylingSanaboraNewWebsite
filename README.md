# sassStylingSanaboraNewWebsite
This is a stylesheet base on SASS then this will be compiled into CSS using the sass commandline it encompasses the typography,Iconography,Layout,Color system,Shape,size and media styling.
